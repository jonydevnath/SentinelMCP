# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "SentinelMCP"
slug: "sentinelmcp"

runtime: python

build:
  install: "uv sync --frozen --no-dev"

start:
  type: http
  command: ".venv/bin/python -m SentinelMCP.server"
